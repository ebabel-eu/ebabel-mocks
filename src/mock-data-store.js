'use strict';

/**
 * `dataStore`
 * Mock dataStore for unit testing purposes.
*/
const mockDataStore = {
  player: {
    creation: {},
    state: {
      position: [0, 0, 0],
      rotation: [0, 0, 0],
    },
  },
};

module.exports = mockDataStore;
