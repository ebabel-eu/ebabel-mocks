'use strict';

/**
 * `dataStore`
 * Mock dataStore for unit testing purposes.
*/
const mockDataStore = {
  player: {
    creation: {
      position: [0, 0, 0],
      rotation: [0, 0, 0],
    },
    state: {
      position: [0, 0, 0],
      rotation: [0, 0, 0],
    },
  },
};

module.exports = mockDataStore;
